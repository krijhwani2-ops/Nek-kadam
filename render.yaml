services:
  - type: web
    name: nekkadam
    runtime: node
    plan: free
    buildCommand: ELECTRON_SKIP_BINARY_DOWNLOAD=1 npm install --include=dev --no-audit --no-fund && npm run build
    startCommand: node server_pg.cjs
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres.quzmtmvymlrwprewszkr:nekkadam2026@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: ELECTRON_SKIP_BINARY_DOWNLOAD
        value: "1"
      - key: NODE_VERSION
        value: "20.18.0"
